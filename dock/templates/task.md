# Task: {title}

## Manifest

| | |
|---|---|
| Slug | `{YYYY-MM-DD}-{slug}` |
| Issue | `{KEY}` / none |
| Repo | `{path}` |
| Base branch | `{base}` |
| Branch | `{branch}` |
| Worktree | `{path}` / none |
| PR | `{url}` / not opened |
| Fast check | `{diff-scoped command}` |
| Full gate | `{command}` — last passed at `{sha}` |
| Project rules | `{AGENTS.md / CLAUDE.md / none}` |

## Goal

One paragraph. What is true after this ships that isn't true now.

## Definition of Done

- [ ] Concrete, checkable criterion.
- [ ] Concrete, checkable criterion.

## Out of Scope

- Named explicitly, so scope creep has something to bounce off.

## Source

Issue body verbatim, or the original prompt.
